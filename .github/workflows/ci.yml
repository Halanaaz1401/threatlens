name: ThreatLens CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-test-and-coverage:
    name: Backend Pytest, Coverage & Security Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Backend Dependencies
        run: |
          cd backend
          python -m pip install --upgrade pip
          pip install pytest pytest-cov httpx pip-audit
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Security Vulnerability Scan (pip-audit)
        run: |
          cd backend
          pip-audit || true

      - name: Run Test Suite with Coverage Gate
        env:
          PYTHONPATH: backend
        run: |
          cd backend
          python -m pytest tests/ -v --cov=app.api.v1.endpoints.export --cov-report=term-missing

  frontend-build:
    name: Frontend Lint & Production Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Frontend Dependencies
        run: |
          cd frontend
          npm install

      - name: Next.js Production Build
        run: |
          cd frontend
          npm run build